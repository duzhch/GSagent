name: gsagent_native
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - fastapi>=0.115,<1.0
      - uvicorn>=0.30,<1.0
      - pydantic>=2.9,<3.0
      - httpx>=0.27,<1.0
      - langgraph>=0.2,<0.3
      - pytest>=8.3,<9.0
